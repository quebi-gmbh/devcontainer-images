{
  "_comment": "Build config for rebuilding devcontainer images from upstream source (escaping MCR). Variants, version, and alias tags are NOT listed here — they're read from each image's upstream src/<src>/manifest.json so we always track the most recent upstream release. The setup job resolves upstream.ref to a commit once per run (logged), and every job in the run uses that exact commit for consistency.",
  "upstream": {
    "repo": "devcontainers/images",
    "ref": "main"
  },
  "baseMirror": "mirror.gcr.io/library",
  "images": [
    {
      "src": "javascript-node",
      "name": "devcontainer-javascript-node",
      "fromKind": "dockerhub-node",
      "comment": "FROM node:<variant> (Docker Hub) -> mirror.gcr.io/library/node:<variant>"
    },
    {
      "src": "typescript-node",
      "name": "devcontainer-typescript-node",
      "fromKind": "javascript-node",
      "needs": "devcontainer-javascript-node",
      "comment": "FROM mcr.../javascript-node:4-<variant> -> our ghcr.io/<owner>/devcontainer-javascript-node:<variant>"
    }
  ]
}
